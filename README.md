my-web-project/
├── .github/
│   └── workflows/          # GitHub Actions (CI/CD tự động deploy)
├── public/                 # Tài nguyên tĩnh (Hình ảnh, Favicon, Fonts)
├── src/                    # Mã nguồn chính
│   ├── assets/             # Images, Stylesheets (CSS/SCSS)
│   ├── components/         # Các thành phần giao diện tái sử dụng
│   ├── pages/              # Các trang chính (Home, Product, Cart)
│   ├── services/           # Gọi API và xử lý dữ liệu
│   └── utils/              # Các hàm hỗ trợ
├── .gitignore              # Bỏ qua node_modules, .env
├── LICENSE                 # Giấy phép mã nguồn (MIT)
├── README.md               # Document chính trình bày dự án
└── package.json            # Khai báo phụ thuộc và scripts
